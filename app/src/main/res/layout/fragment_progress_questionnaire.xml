<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.ProgressQuestionnaireFragment">

    <ProgressBar
        android:id="@+id/progress_questionnaire_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:progress="50"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:paddingStart="30dp"
        android:paddingEnd="30dp"
        android:paddingBottom="10dp"
        android:paddingTop="10dp"
        android:layout_margin="30dp">
    </ProgressBar>

    <TextView
        android:id="@+id/progress_questionnaire_progress_bar_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0 / 5"
        android:textAlignment="center"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@id/progress_questionnaire_progress_bar">
    </TextView>

    <TextView
        android:id="@+id/progress_questionnaire_progress_bar_subtext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/progress_questionnaire_progress_bar_subtext_text"
        android:textAlignment="center"
        android:textSize="20sp"
        android:layout_marginBottom="35dp"
        app:layout_constraintTop_toBottomOf="@id/progress_questionnaire_progress_bar_text">
    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>